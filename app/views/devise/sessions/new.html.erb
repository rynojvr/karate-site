<div class="box center-block offset-bottom-large">
  <ul role="tablist" class="nav nav-tabs nav-justified nav-tabs-justified-xs offset-bottom">
    <li class="active">
      <%= link_to new_user_session_path do %>
        <!-- <h1 class="h4 page-header text-center offset-bottom">Karate Site</h1> -->
        <span class="offset-left">Karate Site</span>
      <% end %>
    </li>
    <li>
      <%= link_to signup_path do %>
        <span class="offset-left">Sign Up</span>
      <% end %>
    </li>
  </ul>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-group">
      <%= f.label :email, class: 'control-label' %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= f.label :password, class: 'control-label'%>
      <%= f.password_field :password, autocomplete: "off", class: 'form-control' %>
    </div>

    <% if devise_mapping.rememberable? -%>
      <hr />
      <div class="form-group">
        <div class="input-group">
          <span class="input-group-addon">
            <%= f.check_box :remember_me %>
          </span>
          <%= f.label :remember_me, class: 'form-control' %>
        </div>
      </div>
    <% end -%>

    <div class="form-group">
      <%= f.submit "Sign in", class: 'btn btn-primary btn-raised btn-block' %>
    </div>
  <% end %>
</div><!--.box .center-block .offset-bottom-lar -->

<!-- <%= render "devise/shared/links" %> -->
